{
  "name": "auto-insta",
  "version": "0.0.1",
  "description": "save record, upload to insta",
  "scripts": {
    "start": "npx sandbox",
    "test": "curl -fsSL https://deno.land/x/install/install.sh | sh && deno test -A src/"
  },
  "dependencies": {
    "@architect/sandbox": "^4.1.1"
  }
}