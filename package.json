{
  "name": "auto-insta",
  "version": "0.0.1",
  "description": "save record, upload to insta",
  "scripts": {
    "start": "npx sandbox",
    "test": "cat /etc/os-release && yum install unzip && deno upgrade --version 1.17.1 && deno test -A src/"
  },
  "dependencies": {
    "@architect/sandbox": "^4.1.1"
  }
}